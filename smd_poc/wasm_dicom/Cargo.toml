[package]
name = "smd_dicom_decoder"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
dicom-object = "0.7"
dicom-pixeldata = { version = "0.7", features = ["image"] }
dicom-core = "0.7"
image = { version = "0.25", default-features = false }
byteorder = "1.5"
